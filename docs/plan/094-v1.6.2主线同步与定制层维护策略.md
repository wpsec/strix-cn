# 094 `v1.6.2` 主线同步与定制层维护策略

## 目标

在保留 `strix-cn` 本地能力的前提下，吸收上游 `main` 的最新修复和新增能力，完成一次可复核的本地合并：

- 保留 Burp / Caido 被动扫描、代理作用域、功能批次和本地 Viewer
- 保留中文 CLI、TUI、报告、错误提示和文档体验
- 保留 `--token-limit` 及 Token 优先级调度、运行记录和恢复能力
- 保留 `--verify` 的 Docker sandbox 确定性复测链路
- 清除本轮确认没有引用的前端旧产物和过期引用

## 同步基线

- 上游远端：`https://github.com/usestrix/strix.git`
- 合并目标：`upstream/main`
- 本轮上游提交：`84f4108195fb516d48745aa912ba8862c7360ebb4`
- 相对共同基线的上游增量：19 个提交
- 发布版本：上游 `v1.6.2`

## 冲突裁决

核心流程、报告链路和代理工具按三方内容复核，不直接用单边文件覆盖：

- 吸收上游的运行阶段 Telemetry、导入预热、MCP/Cloud 修复、Exa Web Search、HTTP 交换证据关联和模型识别修复
- 保留本地 `strix/core/proxy_scope.py`、`strix/core/token_budget.py`、`strix/runtime/proxy_capture.py`、`strix/verification/` 与中文报告链路
- 在 `create_vulnerability_report` / `update_vulnerability_report` 中补齐 `http_exchange_ids`，并继续执行本地证据完整性和 Token 限额校验
- Web Search 默认按 `auto -> Exa -> Perplexity` 选择，可显式固定 Provider；新增配置已同步 README 和配置文档
- Viewer 保持本地扫描视角，拒绝引入上游 Cloud 导航和无引用的 `UpgradeModal` / 旧静态 bundle

## 验收

- `./scripts/merge-guard.sh`：Python 122 项通过，Go TUI 契约通过
- Web Search、MCP resilience、代理作用域、Token budget、验证模式、报告字段定向测试通过
- 本轮完整 Python 回归中发现的 3 项测试问题已分别处理：中文输出断言、MCP MockTransport 受宿主代理变量影响；修复后对应定向测试通过
- `git diff --check` 无空白错误
- `mypy` 和 `bandit` 仍有合并前已存在的基线告警，未将其伪装成“全绿”；无高危 Bandit 发现

## 下次同步规则

1. 先执行 `git fetch upstream --prune`，记录共同基线和上游提交范围。
2. 先处理 `scripts/merge-guard.sh` 覆盖的代理、中文、Token 和 Viewer 契约，再处理普通功能冲突。
3. 上游新增用户可见文案必须同步中文化；新增 Provider 必须同时补配置说明和脱敏测试。
4. 删除文件前先用 `rg` 检查引用；生成的 Viewer 产物必须与 `index.html` 的入口文件名一致。
